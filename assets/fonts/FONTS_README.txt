FONT SETUP INSTRUCTIONS
=======================
These are placeholder stub files. Replace them with the real font files:

1. PlayfairDisplay-Bold.ttf
   → fonts.google.com/specimen/Playfair+Display
   → Download Family → extract PlayfairDisplay-Bold.ttf

2. Nunito-Regular.ttf  (weight 400)
3. Nunito-SemiBold.ttf (weight 600)  
4. Nunito-Bold.ttf     (weight 700)
   → fonts.google.com/specimen/Nunito
   → Download Family → extract the three weights

Place all 4 files in: assets/fonts/
Then run: flutter pub get
